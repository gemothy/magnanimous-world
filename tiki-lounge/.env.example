# Public URL for metadata and sharing. Set this to the final Vercel domain.
NEXT_PUBLIC_SITE_URL=https://lounge.your-domain.com

# Optional private Drive gateway. Without these values the app uses its local demo.
LIBRARY_GATEWAY_URL=https://your-drive-gateway.run.app
LIBRARY_GATEWAY_SECRET=replace-with-a-long-random-secret
